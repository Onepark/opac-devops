[project]
name = "opac-stg-prod-restore-images"
version = "0.1.0"
description = "Restore tooling images for staging production-derived database refresh"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.42.75",
    "psycopg2-binary>=2.9.11",
]
